apiVersion: gateway.solo.io/v1
kind: VirtualService
metadata:
  name: rustfs
  namespace: gloo-system
spec:
  virtualHost:
    domains:
      - rustfs.test.local
    routes:
      - matchers:
          - prefix: /
        routeAction:
          single:
            upstream:
              name: rustfs-rustfs-9001
              namespace: gloo-system
